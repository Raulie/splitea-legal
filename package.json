{
  "name": "splitea-legal",
  "private": true,
  "version": "0.0.0",
  "description": "Splitea legal pages (privacy / terms / support) served via Cloudflare Workers Static Assets",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
